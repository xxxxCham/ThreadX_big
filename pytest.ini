# pytest.ini
[pytest]
minversion = 7.0
addopts = -q --disable-warnings --maxfail=1
testpaths = tests
markers =
    integration: tests d'intégration
    slow: tests lents
    ui: tests UI Dash (P8)
